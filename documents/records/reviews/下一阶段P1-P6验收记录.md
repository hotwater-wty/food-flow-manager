# 下一阶段 P1-P6 验收记录

> 验收日期: 2026-08-28

## P1 路由过渡

- 管理端 `fade-transform` 补齐 `enter-active/leave-active` 的 opacity + transform transition。
- `pnpm type-check`、`pnpm lint`、`pnpm -r build`、`git diff --check` 通过。

## P5/P6 SSE

- 后端新增 `AdminNotificationService`，ticket 使用 UUID、60 秒 TTL、一次性消费；SSE 连接按员工 ID 管理并在关闭/超时/异常时清理。
- `POST /api/admin/notifications/ticket` 使用现有 Bearer JWT；`GET /api/admin/notifications/stream?ticket=...` 仅接受短期 ticket，长期 JWT 不进入 URL。
- 订单创建发布 `new-order`，预约创建发布 `new-reservation`，预约到店发布 `reservation-check-in`。
- 管理端订单工作台建立连接，收到事件后调用静默加载；组件卸载关闭连接。
- `./mvnw test -q`、`pnpm type-check`、`pnpm lint`、`pnpm -r build` 通过。

## 未完成与限制

- 桌码 P2-P4 尚未实施：当前后端没有桌码字段/解析接口，不能用前端假数据代替生产契约。
- SSE 当前 ticket 和连接存于单实例内存；多实例部署前需迁移 Redis/共享事件，并补 nginx 长连接配置。
- 现有 E2E 需要运行中的前后端测试环境；本记录不把未启动环境下的 E2E 视为通过。
