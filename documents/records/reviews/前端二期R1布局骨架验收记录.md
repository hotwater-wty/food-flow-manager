# 前端二期 R1 布局骨架验收记录

> 日期:2026-08-27
> 对应计划:[`../../archive/frontend/plans/frontend-redesign-plan.md`](../../archive/frontend/plans/frontend-redesign-plan.md) 切片 R1
> 环境:docker compose 启动 MySQL/Redis/后端(health UP),前端 `vite` 开发服务器(5173)

## 一、变更清单

| 类别 | 文件 | 说明 |
| --- | --- | --- |
| 新增 | `frontend/src/styles/tokens.css` | 设计令牌(色彩/间距/圆角/布局尺寸 CSS 变量),落实 04 文档陶土橙令牌 |
| 新增 | `frontend/src/layouts/AdminLayout.vue` | 管理端布局:左侧两级菜单树、顶栏页名/员工姓名/角色标签/退出登录;≤760px 折叠为横向菜单 |
| 新增 | `frontend/src/layouts/CustomerLayout.vue` | 顾客端布局:顶栏品牌+右上角账户入口、底部四项导航(首页/点餐/预约/订单) |
| 新增 | `frontend/src/views/NotFoundView.vue` | 404 兜底页 |
| 重写 | `frontend/src/router/index.ts` | 改嵌套路由(布局父路由+页面子路由),URL 全部保持不变;新增 catch-all 404;RouteMeta 增加 `title` |
| 重写 | `frontend/src/style.css` | 全量迁移到设计令牌,配色由工程绿切换为陶土橙暖色,中文字体优先;删除旧全局头部样式 |
| 修改 | `frontend/src/App.vue` | 瘦身为纯 `<RouterView />`,布局职责移交 layouts |
| 修改 | `frontend/src/main.ts` | 先引 tokens.css 再引 style.css |
| 重写 | `frontend/src/views/HomeView.vue` | 过期脚手架文案改为顾客业务入口页(按登录态差异化欢迎语+行动按钮+流程说明+员工入口) |
| 修改 | `frontend/src/views/CustomerLoginView.vue` | 登录成功按 `?redirect` 回跳(默认进点餐页);已登录面板增加"进入点餐" |
| 修改 | `frontend/src/views/AdminLoginView.vue` | 增加"返回顾客端首页"出口 |

## 二、命令级验收

- `./node_modules/.bin/vue-tsc --noEmit` → 退出码 0(环境无全局 pnpm,直接使用本地 bin 等价执行)。
- `./node_modules/.bin/vite build` → 构建成功,产物含新增 NotFoundView 分包。
- `docker compose up -d` → MySQL/Redis healthy,后端 `/actuator/health` 返回 `{"status":"UP"}`。

## 三、浏览器验收(1440x900 与 390x844)

顾客端(桌面+移动):

1. `/` 首页:CustomerLayout 三段式(顶栏/入口卡片/底部导航)渲染正确,陶土橙主色+暖白背景,移动视口无溢出无遮挡;
2. 未登录访问 `/customer/menu` → 重定向 `/customer/login?redirect=/customer/menu`;
3. 顾客登录(13900001234)成功后回跳 `/customer/menu`,顶栏右上角显示昵称"前端验收用户",真实菜品数据加载正常;
4. 底部导航四项,当前页高亮("首页"按精确匹配高亮,避免父路由误高亮)。

公共:

5. `/no-such-page` → 404 页渲染"页面不存在"+两个出口链接;
6. 未登录访问 `/admin/orders` → 重定向 `/admin/login?redirect=/admin/orders`;
7. 两个登录页独立无布局,卡片居中,管理端登录页有"返回顾客端首页"。

管理端(桌面):

8. 店长登录(18800000000)后进入 `/admin/orders`:左侧菜单树(经营工作台:订单处理/会话与桌台;资料维护:资源维护)、当前项陶土橙高亮;
9. 顶栏:页名"订单处理"(随路由联动)、员工名"默认店长"、"店长"角色标签、退出登录按钮;
10. 侧栏点击"会话与桌台"、"资源维护"均可导航,页名与高亮同步;
11. 点击"退出登录" → 清空登录态并回到 `/admin/login`。

管理端(移动 390x844):

12. 侧栏折叠为顶部横向菜单,"订单处理"保持高亮;顶栏员工信息与退出按钮完整可见;订单卡片无横向滚动、无重叠。

## 四、遗留与后续

- 本片只做结构,未引入组件库;表格/抽屉/弹窗等组件化改版在 R2/R3 进行。
- 布局令牌已就绪,但页面内部仍是首版手写控件样式,视觉细节(表格密度、抽屉、图标)待 R2-R4 逐步统一。
- 管理端菜单树中"资料维护"暂为单项,R3 拆分后升级为五个子项。
