# 前端学习型开发总计划

> 文档状态：当前计划
> 维护入口：[`documents/CURRENT.md`](../CURRENT.md)
> 执行规则：[`AGENTS.md`](../../AGENTS.md)
> 前端规格：[`documents/frontend/README.md`](../frontend/README.md)

## 1. 目标与现实边界

本计划面向 Vue 3 + TypeScript 基础尚未完整、但希望通过项目实践快速形成工程能力的学习过程。目标不是先把课程全部学完，而是在真实后端接口上逐步完成一个可演示的顾客端和商户端前端，同时把每次遇到的知识点转化为可复用理解。

当前仓库已有 `frontend/` 工程目录。工程基础阶段已完成，后续按“先任务简报、再选择分工、分块实践、验收复盘”的学习节奏推进。

## 2. 默认节奏

每个切片遵循：

```text
确认业务故事与后端契约
  -> 输出任务简报（流程、验收、注意事项、重点机制、分工建议）
  -> 用户选择自己做、AI 做、分块协作或先补基础
  -> 明确本次唯一目标
  -> 按选择拆成小块实践
  -> 每块解释 Vue/TypeScript/HTTP 知识
  -> 用户运行、阅读和提问
  -> 类型检查、构建、浏览器验收
  -> 更新文档与学习记录
  -> Git 检查点
```

用户说“展开下一步”时只生成任务简报，不自动改代码。讲解先建立当前任务在业务流、工程结构和数据流中的位置，再对用户明确提出或实际卡住的概念按需深入；不强制把每个知识点拆成孤立的固定模板，是否真正理解由用户自行判断。

遇到基础知识缺口时，优先在当前代码中解释其作用，再决定是即时补课还是回看课程。若某个基础缺口会影响后续架构判断，则暂停编码先补齐。

## 3. 阶段路线

### 阶段 0：启动前核对

产出：后端认证契约核对、开发账号/启动条件确认、Node/pnpm 环境确认、前端工程目录约定。

验收：能明确登录接口的请求字段、响应字段、Token 保存方式和错误边界；不修改后端源码。

### 阶段 1：建立前端工程骨架（已完成）

产出：Vue 3 + TypeScript + Vite 工程、路由、Pinia、Axios、环境变量、开发代理、基础类型和最小启动页。

学习重点：目录结构、`createApp`、组件、`ref`/`computed`、路由、Store、异步请求、TypeScript 类型声明。

验收：`pnpm install`、`pnpm type-check`、`pnpm build`、浏览器打开首页均成功。

### 阶段 2：顾客认证切片

产出：顾客登录和注册，Token 持久化，401 返回登录，最小用户状态展示。

当前进度：2.1 顾客登录最小闭环已完成；已接入真实登录接口、表单 loading、业务失败和后端不可用提示。2.2 Token 持久化、认证 Store、401 处理和路由保护尚未开始。

学习重点：表单绑定、事件、响应式状态、Promise、Axios 拦截器、路由守卫。

验收：使用真实后端完成注册或登录，刷新页面仍保持登录，清除 Token 后受保护路由回登录页。

### 阶段 3：顾客预约与开台切片

产出：空闲桌位、预约创建/取消、模拟扫码、当前会话。

学习重点：列表渲染、条件渲染、查询参数、一次性提交令牌、业务状态映射和错误恢复。

### 阶段 4：顾客点餐与订单切片

产出：启售菜品、分类、前端购物车、创建订单、订单列表和详情。

学习重点：跨页面共享状态、数组与对象类型、金额展示、提交状态和后端分页/非分页边界。

### 阶段 5：商户订单与会话切片

产出：员工登录、订单工作台、状态推进、会话列表、取消等待和清台。

学习重点：管理端布局、表格/分页、角色权限、轮询或手动刷新、状态机约束。

### 阶段 6：第二批资料与工程收尾

产出：桌位、菜品、分类、员工管理等第二批页面；补齐测试、构建、浏览器验收、文档和复盘。

## 4. 每次任务的固定格式

```text
背景：当前用户故事和已有接口
唯一目标：本轮只完成什么
知识重点：用户需要理解的 Vue/TypeScript/HTTP 概念
允许修改：明确文件或目录
禁止修改：无关模块、后端核心规则、未经讨论的视觉与依赖
验收：命令、浏览器路径、成功场景、失败场景
交付：代码、解释、验证结果、待补基础、下一检查点
```

默认分工建议：用户亲手完成页面核心结构、响应式状态、关键事件、核心接口调用、状态/错误处理和至少一次独立复现；AI 负责脚手架、重复配置、类型初稿、诊断、验收辅助和文档同步。用户可以覆盖此建议。

## 5. 当前首个工作项

当前工作项是“阶段 2：顾客认证切片任务简报”。开始执行前先核对下列后端认证事实：

| 场景 | 请求 | 成功响应 |
| --- | --- | --- |
| 顾客注册 | `POST /api/user/auth/register`，`phone/password/nickname` | `userId/phone/nickname/status` |
| 顾客登录 | `POST /api/user/auth/login`，`phone/password` | `userId/phone/nickname/status/token` |
| 员工登录 | `POST /api/admin/auth/login`，`phone/password` | `employeeId/phone/name/role/status/token` |

统一外层响应为 `Result<T>`：`code=1` 成功，`code=0` 业务失败；认证失败还可能返回 HTTP 401，角色不匹配返回 HTTP 403。前端第一阶段不能假定存在短信登录，也不能把员工注册误当作公开注册。

当前状态：阶段 0 环境核对、阶段 1 工程骨架和阶段 2.1 顾客登录最小闭环已完成；下一步是先输出 2.2 Token 持久化与认证状态任务简报。

## 6. 需要用户参与的检查点

阶段 0 环境核对结果（2026-08-22）：

- Node.js：`v22.23.2`，可用。
- pnpm：`11.19.0`，可用。
- 根目录 `frontend/`：Vue 3 + TypeScript + Vite 工程已初始化，顾客登录接口已接入，尚未完成全局认证状态。
- 后端 `http://localhost:8080/actuator/health`：当前不可访问，服务未启动。
- Docker Compose：当前 Docker daemon 不可用；不能把容器启动当作已验证前置条件。
- 本地开发配置依赖 MySQL `localhost:3306` 和 Redis `localhost:6379`。Redis 地址已从旧 Windows 虚拟机局域网地址切换为 Mac 本机地址，但两个依赖服务当前均未验证可访问；启动后端前需先确认其运行状态和凭据。

在进入阶段 2 前，需要由用户确认后端依赖的启动方式或先启动 MySQL、Redis 并让后端可访问认证接口。Node.js/pnpm 已满足前端开发条件；没有真实后端时只能做静态准备，不能完成登录验收。

## 7. 计划更新规则

- 每完成一个阶段，更新状态、实际产出和遗留问题。
- 如果后端契约发生变化，先更新接口事实和本计划，再修改前端。
- 用户明确改变节奏或范围时，保留旧计划作为历史记录，并更新本文件当前版本。

## 8. 学习工作流调整记录

2026-08-23：个人 Skill `project-practice-learning` 已封存并停止自动触发。实践表明，其双层解释模板适合单个陌生概念的首次上手，但不足以串联多个知识点并建立总体工程观。当前项目继续保留“先任务简报、用户决定分工、分块实践、验收复盘”的流程；讲解方式改为先建立工程地图和当前切片位置，再按实际卡点使用概念解释。详细问题记录见 [`../records/issues/项目实践学习Skill封存记录.md`](../records/issues/项目实践学习Skill封存记录.md)。
