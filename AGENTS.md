# 项目协作规则

## 适用范围

本文件适用于整个 `food-flow-manager` 仓库及其前端建设任务。开始任何编码、文档或联调任务前，先阅读本文件、`documents/CURRENT.md`、相关当前事实文档和任务指定的契约。

## 用户的前端学习型开发方式

- 用户已学习 Vue 3 + TypeScript 基础，但知识尚不完整；默认采用“项目实践带动基础补足”的节奏。
- 用户说“展开下一步”时，默认只输出任务简报，不代表授权编码；必须先让用户选择由自己完成、AI 完成、分块协作或先补基础。
- 前端工作以真实业务闭环推进。一个任务只实现一个可验证的小目标，不一次生成完整应用或整套页面。
- 每次编码后必须解释：改了什么、为什么这样组织、涉及哪些 Vue/TypeScript/HTTP 知识、用户还需要补哪些课程知识。
- 用户遇到不理解的知识点时，先提供当前项目中的最小解释和可运行例子，再建议回看课程；不要用大段脱离项目的理论替代实践。
- 用户没有明确要求时，不先讨论个性化视觉样式；优先处理工程基础、接口契约、状态和业务流程。
- 默认建议用户亲手完成核心最小闭环，AI 负责脚手架、重复配置、类型初稿、检查辅助和文档同步；最终分工由用户决定。

## AI 与 Agent 的行为边界

- 先读源码和当前文档，再提出或执行变更；不能把历史计划、枚举值或猜测当成当前事实。
- 不得为了让页面“跑起来”伪造长期假接口、绕过鉴权、修改后端业务语义或隐瞒契约缺口。
- 每次任务必须说明目标、范围、允许修改的文件、验收方式和已知风险。
- 每次任务开始前先说明操作流程、验收标准、注意事项、重点机制和 AI/用户分工建议；未得到执行选择前不修改代码。
- 一次只推进一个切片或一个基础能力；完成后停止在可验收检查点，等待用户理解和确认下一步。
- 讲解应先说明当前任务在业务流、工程结构和数据流中的位置，再按用户实际卡点补充概念解释；不强制对每个知识点套用固定模板，也不替用户判断其是否真正理解。
- 如果用户要求会导致明显技术债、契约不一致、不可验证或超出当前范围，应指出问题并先协商；不得默默照做。
- 可以自主选择实现细节，但不能自主扩大业务范围、引入无必要依赖或批量重构无关模块。
- 任何 Agent 接手任务时，必须先读取本文件和当前计划；交付时报告文件、命令、浏览器路径或其他可复现证据。

## 开发工作流

1. 确认当前状态和真实后端契约。
2. 将用户故事拆成一个最小切片。
3. 写清本次任务说明和验收条件。
4. 分块实现，每块解释后再继续。
5. 运行类型检查、构建和必要的浏览器验收。
6. 更新当前文档、学习记录和风险。
7. 建立 Git 检查点，再开始下一切片。

## 文档与 Git

- 当前事实写入 `documents/architecture/` 或 `documents/frontend/`；当前阶段计划写入 `documents/planning/`；过程证据写入 `documents/records/`。
- 每次前端切片完成后更新 `documents/planning/frontend-development-plan.md`，必要时补充接口契约和学习记录。
- 不修改 `backend/src`、`backend/pom.xml`、Docker 或数据库脚本，除非任务明确授权且先说明原因。
- 提交信息使用清晰的中文或英文动词短语；提交前运行 `git diff --check` 和与任务匹配的验证命令。
